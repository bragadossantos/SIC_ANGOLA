---
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
---

<!doctype html>
<html lang="pt" class="scroll-smooth">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Denúncia Anónima | SIC Angola</title>
    <script defer src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js"></script>
  </head>
  <body class="bg-gray-50 dark:bg-sic-blue text-gray-900 dark:text-gray-100 min-h-screen flex flex-col">

    <Header />

    <main class="flex-grow container mx-auto px-6 py-12">
      <h1 class="text-4xl md:text-5xl font-bold text-center mb-8 text-sic-red">Denúncia Anónima</h1>
      <p class="text-center text-lg mb-12 max-w-3xl mx-auto">
        Envie a sua denúncia de forma completamente anónima. Não pedimos dados pessoais. Todas as informações são tratadas com confidencialidade absoluta.
      </p>

      <form 
        x-data="{ submitting: false }"
        @submit.prevent="submitting = true; // Aqui vai fetch real depois"
        class="bg-white dark:bg-sic-blue-light p-8 rounded-xl shadow-lg max-w-2xl mx-auto space-y-6"
      >
        <div>
          <label class="block text-sm font-medium mb-2">Tipo de Crime / Ocorrência *</label>
          <select required class="w-full p-3 rounded border dark:border-gray-600 dark:bg-sic-blue text-gray-900 dark:text-white">
            <option value="">Selecione...</option>
            <option>Roubo / Furto</option>
            <option>Tráfico de drogas</option>
            <option>Violência doméstica</option>
            <option>Corrupção</option>
            <option>Outros</option>
          </select>
        </div>

        <div>
          <label class="block text-sm font-medium mb-2">Descrição detalhada *</label>
          <textarea required rows="6" class="w-full p-3 rounded border dark:border-gray-600 dark:bg-sic-blue text-gray-900 dark:text-white"></textarea>
        </div>

        <div>
          <label class="block text-sm font-medium mb-2">Local da ocorrência (opcional)</label>
          <input type="text" placeholder="Ex: Bairro, Município, Luanda" class="w-full p-3 rounded border dark:border-gray-600 dark:bg-sic-blue text-gray-900 dark:text-white">
        </div>

        <div>
          <label class="block text-sm font-medium mb-2">Anexar provas (fotos/vídeos - opcional, anónimo)</label>
          <input type="file" multiple accept="image/*,video/*" class="w-full p-3 rounded border dark:border-gray-600 dark:bg-sic-blue">
          <p class="text-xs text-gray-500 mt-1">Ficheiros serão processados de forma segura e anónima.</p>
        </div>

        <!-- CAPTCHA placeholder (usa hCaptcha/Turnstile depois) -->
        <div class="bg-gray-200 dark:bg-gray-700 p-4 rounded text-center">
          [CAPTCHA aqui - ex: Cloudflare Turnstile]
        </div>

        <button 
          type="submit" 
          x-bind:disabled="submitting"
          class="w-full bg-sic-red hover:bg-red-700 text-white font-bold py-4 px-8 rounded-lg text-lg transition disabled:opacity-50"
        >
          <span x-show="!submitting">Enviar Denúncia Anónima</span>
          <span x-show="submitting">A enviar...</span>
        </button>
      </form>

      <p class="text-center mt-8 text-sm text-gray-600 dark:text-gray-400">
        Em caso de emergência, ligue imediatamente 111.
      </p>
    </main>

    <Footer />
  </body>
</html>